package com.just.networking.impl.frame.channel;

public class TCPFrameChannelConstants {

    // Length prefix is a 4-byte big-endian int that counts bytes after itself.
    public static final int LEN_BYTES = Integer.BYTES;
}
